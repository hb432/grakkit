// src/platform/paper/command.ts

export const command = ({ name, permission, message, execute, tabComplete }: any) => {
    // Simplified implementation for demonstration
    console.log(`Command registered: ${name}`);
};